{
  "name": "stream-weaver-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx server/dev.ts",
    "build": "npm run build:client && npm run build:fix-paths && npm run build:server",
    "build:client": "vite build",
    "build:fix-paths": "node scripts/fix-paths.js",
    "build:server": "vite build --ssr src/pages.ts --outDir dist/server",
    "preview": "tsx server/prod.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "stream-weaver": "file:.."
  },
  "devDependencies": {
    "@types/node": "^24.0.6",
    "tsx": "^4.21.0",
    "typescript": "~5.7.3",
    "vite": "^7.3.1"
  }
}
