/**
 * Server-side double logic - multiplies a value by 2
 * This would typically contain server-specific code like DB access
 * @param {number} count - Raw signal value
 * @returns {number} The doubled value
 */
export default (count) => count * 2;
